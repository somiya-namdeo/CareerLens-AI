def clean_text(text: str) -> str:
    """
    Cleans raw resume text for model inference.
    """
    # Placeholder for actual text cleaning logic (removing special chars, lowercasing, etc.)
    return text.lower().strip()
